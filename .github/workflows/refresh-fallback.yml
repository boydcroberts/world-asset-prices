name: Refresh fallback snapshot

on:
  schedule:
    # 06:30 UTC daily — well after US market close, before next-day open
    - cron: "30 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Refresh per-stock daily stats (prev close + 52-week range)
        run: node ./scripts/refresh-universe-stats.mjs

      - name: Refresh fallback snapshot
        run: node ./scripts/refresh-fallback.mjs

      - name: Commit and push if changed
        run: |
          set -e
          if git diff --quiet server/fallback/dashboard-fallback.json server/data/universe-stats.json; then
            echo "No changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/fallback/dashboard-fallback.json server/data/universe-stats.json
          git commit -m "chore(data): daily refresh of fallback + stock stats"
          git push
